@model JAMSv1._0.Models.Quiz

@{
    ViewBag.Title = "Index";
}

<div id="progressbar">
    <div id="progressbar" class="all-rounded" style="width: 80%"><span></span></div>
</div>

<div class="row">
    <div class="col-md-offset-1">
        <section id="questionForm">
            <h2>Questions</h2>


            @using (Html.BeginForm())
            {
                @Html.EditorFor(x => x.Questions)
                <input type="submit" id="submit" class="btn btn-default" value="Submit" />
            }

            @*@Html.ActionLink("Create New", "Create")*@

            @*@using (Html.BeginForm("Index", "Questions", FormMethod.Post, new { enctype = "multipart/form-data" }))
                {
                    @Html.AntiForgeryToken()
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    <div class="form-group">
                        <p>
                            @Html.DisplayNameFor(model => model.QuestionString)
                            @{ViewBag.QuestionNumber = i + 1 + ":";}
                            @ViewBag.QuestionNumber&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
                            @Html.DisplayFor(modelItem => x.QuestionString)
                        </p>
                    </div>


                    <div class="form-group">
                        <p>
                            @Html.RadioButtonFor(modelItem => item.SelectedAnswer, item.Answer1)
                            @Html.LabelFor(modelitem => item.Answer1, item.Answer1)
                        </p>
                    </div>
                    <div class="form-group">
                        <p>
                            @Html.RadioButtonFor(modelItem => item.SelectedAnswer, item.Answer2)
                            @Html.LabelFor(modelitem => item.Answer2, item.Answer2)
                        </p>
                    </div>
                    <div class="form-group">
                        <p>
                            @Html.RadioButtonFor(modelItem => item.SelectedAnswer, item.Answer3)
                            @Html.LabelFor(modelItem => item.Answer3, item.Answer3)
                        </p>
                    </div>
                    <div class="form-group">
                        <p>
                            @Html.RadioButtonFor(modelItem => item.SelectedAnswer, item.Answer4)
                            @Html.LabelFor(modelItem => item.Answer4, item.Answer4)
                        <p>&nbsp;</p>
                        </p>
                    </div>

                }

                using (Html.BeginForm("Index", "Questions", FormMethod.Post, new { enctype = "multipart/form-data" }))
                {
                <div class="form-group">
                    <p>
                        <input type="submit" id="submit" class="btn btn-default" value="Submit" />
                    </p>
                </div>
                }
                }*@
        </section>
    </div>
</div>
